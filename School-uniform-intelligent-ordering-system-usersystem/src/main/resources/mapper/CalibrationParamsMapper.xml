<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.authguard.usersystem.mapper.CalibrationParamsMapper">

    <resultMap id="CalibrationParamsMap" type="com.authguard.usersystem.entity.CalibrationParams">
        <id property="paramId" column="param_id"/>
        <result property="scopeType" column="scope_type"/>
        <result property="scopeId" column="scope_id"/>
        <result property="targetSizeId" column="target_size_id"/>
        <result property="calibrationType" column="calibration_type"/>
        <result property="adjustmentValue" column="adjustment_value"/>
        <result property="isManual" column="is_manual"/>
        <result property="enabled" column="enabled"/>
        <result property="status" column="status"/>
        <result property="sampleSize" column="sample_size"/>
        <result property="uniqueUserCount" column="unique_user_count"/>
        <result property="feedbackDistribution" column="feedback_distribution"/>
        <result property="confidenceLevel" column="confidence_level"/>
        <result property="effectiveFrom" column="effective_from"/>
        <result property="effectiveUntil" column="effective_until"/>
        <result property="version" column="version"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="selectCalibrationParamsColumns">
        param_id,
        scope_type,
        scope_id,
        target_size_id,
        calibration_type,
        adjustment_value,
        is_manual,
        enabled,
        status,
        sample_size,
        unique_user_count,
        feedback_distribution,
        confidence_level,
        effective_from,
        effective_until,
        version,
        create_by,
        create_time,
        update_by,
        update_time
    </sql>

    <insert id="insert" parameterType="com.authguard.usersystem.entity.CalibrationParams" useGeneratedKeys="true" keyProperty="paramId">
        INSERT INTO calibration_params (
            scope_type,
            scope_id,
            target_size_id,
            calibration_type,
            adjustment_value,
            is_manual,
            enabled,
            status,
            sample_size,
            unique_user_count,
            feedback_distribution,
            confidence_level,
            effective_from,
            effective_until,
            version,
            create_by,
            create_time,
            update_by,
            update_time
        ) VALUES (
            #{scopeType},
            #{scopeId},
            #{targetSizeId},
            #{calibrationType},
            #{adjustmentValue},
            #{isManual},
            #{enabled},
            #{status},
            #{sampleSize},
            #{uniqueUserCount},
            #{feedbackDistribution},
            #{confidenceLevel},
            #{effectiveFrom},
            #{effectiveUntil},
            #{version},
            #{createBy},
            #{createTime},
            #{updateBy},
            #{updateTime}
        )
    </insert>

    <update id="update" parameterType="com.authguard.usersystem.entity.CalibrationParams">
        UPDATE calibration_params
        SET scope_type = #{scopeType},
            scope_id = #{scopeId},
            target_size_id = #{targetSizeId},
            calibration_type = #{calibrationType},
            adjustment_value = #{adjustmentValue},
            is_manual = #{isManual},
            enabled = #{enabled},
            status = #{status},
            sample_size = #{sampleSize},
            unique_user_count = #{uniqueUserCount},
            feedback_distribution = #{feedbackDistribution},
            confidence_level = #{confidenceLevel},
            effective_from = #{effectiveFrom},
            effective_until = #{effectiveUntil},
            version = #{version},
            update_by = #{updateBy},
            update_time = #{updateTime}
        WHERE param_id = #{paramId}
    </update>

    <select id="selectById" resultMap="CalibrationParamsMap">
        SELECT <include refid="selectCalibrationParamsColumns"/>
        FROM calibration_params
        WHERE param_id = #{paramId}
    </select>

    <select id="selectActiveByScope" resultMap="CalibrationParamsMap">
        SELECT <include refid="selectCalibrationParamsColumns"/>
        FROM calibration_params
        <where>
            scope_type = #{scopeType}
            <if test="scopeId == null">
                AND scope_id IS NULL
            </if>
            <if test="scopeId != null">
                AND scope_id = #{scopeId}
            </if>
            <if test="targetSizeId == null">
                AND target_size_id IS NULL
            </if>
            <if test="targetSizeId != null">
                AND target_size_id = #{targetSizeId}
            </if>
            AND enabled = TRUE
            AND effective_from &lt;= #{now}
            AND (effective_until IS NULL OR effective_until &gt;= #{now})
        </where>
        ORDER BY version DESC, update_time DESC, create_time DESC
    </select>

    <select id="selectLatestVersion" resultType="java.lang.Integer">
        SELECT COALESCE(MAX(version), 0)
        FROM calibration_params
        <where>
            scope_type = #{scopeType}
            <if test="scopeId == null">
                AND scope_id IS NULL
            </if>
            <if test="scopeId != null">
                AND scope_id = #{scopeId}
            </if>
            <if test="targetSizeId == null">
                AND target_size_id IS NULL
            </if>
            <if test="targetSizeId != null">
                AND target_size_id = #{targetSizeId}
            </if>
        </where>
    </select>
</mapper>
